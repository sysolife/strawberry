<%= form_for(product_comment) do |f| %>
  <% if product_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_comment.errors.count, "error") %> prohibited this product_comment from being saved:</h2>

      <ul>
      <% product_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
